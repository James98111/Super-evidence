const DATA_URL='./data/options.json';
let db;
let state={view:'home',fund:null,option:null,topic:'intro'};
const $=s=>document.querySelector(s);
const money=v=>new Intl.NumberFormat('en-AU',{style:'currency',currency:'AUD',maximumFractionDigits:0}).format(v);
const pct=v=>`${Number(v).toFixed(2).replace(/\.00$/,'')}%`;
const options=()=>db?.options||[];
const option=id=>options().find(o=>o.id===id);
const funds=()=>[...new Map(options().map(o=>[o.fund,o])).values()];
const balance=()=>Number(localStorage.getItem('se_balance')||100000);
function fee(o,b){const c=o.costs;const adminPct=c.adminPercent.capBalance?Math.min(b,c.adminPercent.capBalance)*c.adminPercent.value/100:Math.min(b*c.adminPercent.value/100,c.adminPercent.annualCap??Infinity);const fixed=c.adminFixedAnnual.value;const reserve=b*c.reservePercent.value/100;const invest=b*c.investmentAndTransactionPct.value/100;return{adminPct,fixed,reserve,invest,total:adminPct+fixed+reserve+invest};}
function go(view,params={}){state={...state,view,...params};render();window.scrollTo({top:0,behavior:'smooth'});}
function setActiveNav(){document.querySelectorAll('[data-view]').forEach(b=>b.classList.toggle('active',b.dataset.view===state.view));}
function sourceStatus(m){return m?.status==='issuer_verified'?'Verified':'Cross-check';}
function sortedRankings(){return [...options()].sort((a,b)=>b.performance['10y'].value-a.performance['10y'].value);}
function searchResults(q){q=q.trim().toLowerCase();if(!q)return'';const matches=options().filter(o=>[o.fund,o.product,o.option,`${o.fund} ${o.option}`].some(x=>(x||'').toLowerCase().includes(q))).slice(0,10);if(!matches.length)return'<div class="empty">No match in the prototype yet. Full-market coverage is the target.</div>';return matches.map(o=>`<button class="result-button" data-open-option="${o.id}"><span><strong>${o.fund} — ${o.option}</strong><small>${o.product} · ${o.style}</small></span><span class="result-kind">Investment option</span></button>`).join('');}
function home(){const ranked=sortedRankings();return `<div class="page"><section class="hero"><div class="hero-copy"><span class="eyebrow">Independent Australian super research</span><h1>Understand your super. See how it compares.</h1><p class="lead">Search any fund or investment option, see what you actually own, what it costs, how it has performed and how it compares with genuinely similar options.</p><div class="hero-search"><label class="search-label" for="homeSearch">Find your fund or investment option</label><div class="search-field"><input id="homeSearch" autocomplete="off" placeholder="Try ‘ART High Growth’ or ‘Aware Super’"><span>⌕</span></div><div id="homeResults" class="inline-results" hidden></div></div><p class="coverage-note">Prototype data: ${funds().length} funds · ${options().length} investment options. Built to scale to the full Australian market.</p></div><aside class="start-panel"><span class="eyebrow">Start here</span><h2>What would you like to do?</h2><div class="start-list"><button class="start-action" data-view="my"><span class="num">01</span><span><strong>Help me understand my super</strong><small>Find your fund, option, fees and performance step by step.</small></span><span class="arrow">→</span></button><button class="start-action" data-view="compare"><span class="num">02</span><span><strong>Compare investment options</strong><small>Put similar options side by side without drowning in data.</small></span><span class="arrow">→</span></button><button class="start-action" data-view="rankings"><span class="num">03</span><span><strong>See the rankings</strong><small>Explore stronger returns or lower fees among comparable options.</small></span><span class="arrow">→</span></button></div></aside></section><div class="quick-strip"><div class="quick-item"><strong>Every option, eventually</strong><p>Fund → product → investment option, with complete market coverage.</p></div><div class="quick-item"><strong>Compare like with like</strong><p>Risk and growth exposure come before rankings, so labels do not mislead.</p></div><div class="quick-item"><strong>Every number has a source</strong><p>Issuer evidence, dates and verification status stay attached to the metric.</p></div></div><section class="section"><div class="section-head"><div><span class="eyebrow">Rankings preview</span><h2>High Growth options currently loaded</h2></div><p>Rankings are useful only when the options are genuinely comparable. Full-market rankings will screen by risk, growth exposure, product type and history.</p></div><div class="table-wrap"><table><thead><tr><th>#</th><th>Investment option</th><th>10-year return</th><th>Growth assets</th><th>Investment cost</th><th></th></tr></thead><tbody>${ranked.map((o,i)=>`<tr><td class="rank">${i+1}</td><td class="name-cell"><strong>${o.fund}</strong><small>${o.option}</small></td><td class="number">${pct(o.performance['10y'].value)} p.a.</td><td class="number">${o.growth}%</td><td class="number">${pct(o.costs.investmentAndTransactionPct.value)}</td><td><button class="text-link" data-open-option="${o.id}">View</button></td></tr>`).join('')}</tbody></table></div></section></div>`;}
function mySuper(){const saved=localStorage.getItem('se_option');const selected=saved?option(saved):null;return `<div class="page narrow"><div class="page-head"><span class="eyebrow">My super</span><h1>Let’s work out what you actually have.</h1><p class="lead">You do not need to know the jargon. Start with your fund and we will narrow it down.</p></div><div class="wizard"><div class="wizard-step"><span class="step-number">1</span><div><h3>Which fund are you with?</h3><p class="muted">Check a payslip, your fund app or myGov/ATO if you are unsure.</p><div class="choices">${funds().map(f=>`<button class="choice" data-pick-fund="${f.fund}">${f.fund}</button>`).join('')}<button class="choice">I don’t know</button></div></div></div><div class="wizard-step"><span class="step-number">2</span><div><h3>Which investment option are you in?</h3><p class="muted">Often called Balanced, High Growth, Indexed or a lifecycle option.</p><div id="myOptionChoices" class="choices"><span class="muted">Choose a fund first.</span></div></div></div><div class="wizard-step"><span class="step-number">3</span><div><h3>What is your super balance?</h3><div class="field" style="max-width:220px"><label for="balanceInput">Current balance</label><input id="balanceInput" type="number" min="0" step="1000" value="${balance()}"></div></div></div></div>${selected?`<section class="section"><span class="eyebrow">Saved super</span><h2>${selected.fund} — ${selected.option}</h2><p class="muted">Your saved balance is ${money(balance())}. Estimated disclosed annual cost: <strong>${money(fee(selected,balance()).total)}</strong>.</p><button class="primary-btn" data-open-option="${selected.id}">View my investment option</button></section>`:''}</div>`;}
function rankings(){const ranked=sortedRankings();return `<div class="page"><div class="page-head"><span class="eyebrow">Rankings</span><h1>Compare genuinely similar investment options.</h1><p class="lead">A High Growth label is not enough. Rankings should first match options by growth exposure, product type, management style and available history.</p></div><div class="filters"><div class="field"><label>Category</label><select><option>High Growth (prototype)</option></select></div><div class="field"><label>Period</label><select><option>10 years</option><option>5 years</option><option>3 years</option></select></div><div class="field"><label>Sort by</label><select><option>Return</option><option>Investment cost</option><option>Growth assets</option></select></div></div><div class="callout">Prototype rankings currently include only the loaded options. Full-market ranking will not mix materially different risk profiles.</div><div class="table-wrap"><table><thead><tr><th>#</th><th>Option</th><th>10-year</th><th>5-year</th><th>Growth</th><th>Cost</th><th>Evidence</th></tr></thead><tbody>${ranked.map((o,i)=>`<tr><td class="rank">${i+1}</td><td class="name-cell"><button class="text-link" data-open-option="${o.id}">${o.fund}<br><small>${o.option}</small></button></td><td class="number">${pct(o.performance['10y'].value)}</td><td class="number">${pct(o.performance['5y'].value)}</td><td class="number">${o.growth}%</td><td class="number">${pct(o.costs.investmentAndTransactionPct.value)}</td><td><span class="verified">${sourceStatus(o.performance['10y'])}</span></td></tr>`).join('')}</tbody></table></div></div>`;}
function fundsPage(){const groups=new Map();options().forEach(o=>{if(!groups.has(o.fund))groups.set(o.fund,[]);groups.get(o.fund).push(o);});return `<div class="page"><div class="page-head"><span class="eyebrow">Funds</span><h1>Every fund. Every investment option.</h1><p class="lead">Start with the provider, then drill into the products and investment options available within it.</p></div><div class="fund-list">${[...groups.entries()].map(([fund,opts])=>`<div class="fund-row"><div><h3>${fund}</h3><p>${opts[0].product}</p></div><div class="fund-options">${opts.map(o=>o.option).join(' · ')}</div><button class="text-link" data-open-option="${opts[0].id}">View options →</button></div>`).join('')}</div></div>`;}
function compare(){const a=option(state.compareA)||options()[0],b=option(state.compareB)||options()[1]||options()[0];if(!a||!b)return'';const fa=fee(a,balance()),fb=fee(b,balance());const diff=a.performance['10y'].value-b.performance['10y'].value;const winner=diff===0?'Neither option':diff>0?a.fund:b.fund;return `<div class="page"><div class="page-head"><span class="eyebrow">Compare</span><h1>Side by side, without the noise.</h1><p class="lead">Start with the handful of facts that matter most, then open deeper detail only when you need it.</p></div><div class="compare-picker"><div class="field"><label>Option one</label><select id="compareA">${options().map(o=>`<option value="${o.id}" ${o.id===a.id?'selected':''}>${o.fund} — ${o.option}</option>`).join('')}</select></div><div class="vs">vs</div><div class="field"><label>Option two</label><select id="compareB">${options().map(o=>`<option value="${o.id}" ${o.id===b.id?'selected':''}>${o.fund} — ${o.option}</option>`).join('')}</select></div></div><div class="comparison"><div class="compare-row"><div class="compare-label"></div><div><strong>${a.fund}</strong><br><span class="muted">${a.option}</span></div><div><strong>${b.fund}</strong><br><span class="muted">${b.option}</span></div></div>${[['10-year return',`${pct(a.performance['10y'].value)} p.a.`,`${pct(b.performance['10y'].value)} p.a.`],['Growth assets',`${a.growth}%`,`${b.growth}%`],['Risk',a.risk,b.risk],['Suggested timeframe',a.suggestedTimeframe,b.suggestedTimeframe],['Est. annual cost',money(fa.total),money(fb.total)]].map(r=>`<div class="compare-row"><div class="compare-label">${r[0]}</div><div class="number">${r[1]}</div><div class="number">${r[2]}</div></div>`).join('')}</div><div class="interpretation"><strong>What stands out</strong><p>${winner} has the higher published 10-year return in this prototype. The options are similar, not identical: ${a.fund} reports ${a.growth}% growth assets and ${b.fund} reports ${b.growth}%. Historical returns are descriptive, not a recommendation.</p></div><div class="accordion"><details><summary>Returns and evidence</summary><div class="detail-body">${['1y','3y','5y','10y'].map(k=>`<div class="data-row"><span>${k.replace('y',' year')} return</span><strong>${pct(a.performance[k].value)} vs ${pct(b.performance[k].value)}</strong></div>`).join('')}</div></details><details><summary>Fees at my balance</summary><div class="detail-body"><div class="data-row"><span>${a.fund}</span><strong>${money(fa.total)}/year</strong></div><div class="data-row"><span>${b.fund}</span><strong>${money(fb.total)}/year</strong></div></div></details><details><summary>Portfolio and risk</summary><div class="detail-body"><div class="data-row"><span>${a.fund}</span><strong>${a.growth}% growth / ${a.defensive}% defensive</strong></div><div class="data-row"><span>${b.fund}</span><strong>${b.growth}% growth / ${b.defensive}% defensive</strong></div></div></details></div></div>`;}
function optionPage(id){const o=option(id)||options()[0];const f=fee(o,balance());const strategic=o.strategic['2026']||Object.values(o.strategic).at(-1);return `<div class="page"><div class="breadcrumb"><button data-view="funds">Funds</button> / ${o.fund} / ${o.product}</div><section class="option-header"><div class="option-header-row"><div><span class="eyebrow">Investment option</span><h1>${o.option}</h1><p class="option-summary">${o.fund}'s ${o.option} is a ${o.style.toLowerCase()} option with ${o.growth}% of assets classified as growth. The fund suggests an investment timeframe of ${o.suggestedTimeframe}.</p></div><button class="secondary-btn" data-compare-option="${o.id}">Compare this option</button></div><div class="metric-strip"><div class="metric"><label>10-year return</label><strong>${pct(o.performance['10y'].value)}</strong><small>p.a. to 30 Jun 2026</small></div><div class="metric"><label>Estimated annual cost</label><strong>${money(f.total)}</strong><small>at ${money(balance())}</small></div><div class="metric"><label>Growth assets</label><strong>${o.growth}%</strong><small>${o.defensive}% defensive</small></div><div class="metric"><label>Risk</label><strong>${o.risk}</strong><small>Fund disclosure</small></div><div class="metric"><label>Suggested timeframe</label><strong>${o.suggestedTimeframe}</strong><small>Fund guidance</small></div></div></section><div class="accordion"><details open><summary>How has it performed?</summary><div class="detail-body">${['1y','3y','5y','10y'].map(k=>`<div class="data-row"><span>${k.replace('y',' year')} return</span><strong>${pct(o.performance[k].value)} <small class="verified">${sourceStatus(o.performance[k])}</small></strong></div>`).join('')}<div class="callout">Longer periods are generally more useful for long-term options, but past performance does not predict future results.</div></div></details><details><summary>What does your money actually own?</summary><div class="detail-body"><div class="data-row"><span>Growth assets</span><strong>${o.growth}%</strong></div><div class="data-row"><span>Defensive assets</span><strong>${o.defensive}%</strong></div>${strategic.allocation.filter(x=>x[1]>0).map(([name,v])=>`<div class="data-row"><span>${name}</span><strong>${pct(v)}</strong></div>`).join('')}</div></details><details><summary>What could it cost?</summary><div class="detail-body"><div class="data-row"><span>Estimated disclosed annual cost</span><strong>${money(f.total)}</strong></div><div class="data-row"><span>Investment + transaction costs</span><strong>${pct(o.costs.investmentAndTransactionPct.value)}</strong></div><div class="callout">Insurance, advice and member-specific activity fees are excluded from this estimate.</div></div></details><details><summary>How has the option changed?</summary><div class="detail-body">${o.history.map(e=>`<div class="data-row"><span><strong>${e.date}</strong><br>${e.title}</span><span class="muted">${e.severity}</span></div>`).join('')}</div></details><details><summary>Sources and verification</summary><div class="detail-body"><p class="muted">Every displayed metric is tied to a dated public source. The production evidence ledger will expose the exact source, reporting date, calculation method and verification status for each number.</p></div></details></div></div>`;}
const topics={intro:['Super in 5 minutes','Super is money invested for your retirement. Your employer contributes to a super fund, and that fund invests your balance. The investment option you are in can matter just as much as the fund name.'],fund:['Fund vs investment option','Your fund is the provider. Your investment option is how your money is invested inside that fund. Two people in the same fund can therefore have very different risk and return outcomes.'],growth:['What does High Growth mean?','High Growth generally means more exposure to shares and other growth assets. That can mean larger short-term falls, but the option is designed for longer investment horizons.'],fees:['How fees work','Fees come in different forms: fixed administration fees, percentage administration fees, investment costs, transaction costs and sometimes performance-related costs. Dollar estimates are usually easier to understand than percentages alone.'],returns:['How to read returns','One-year returns tell you what happened recently. Five- and ten-year annualised returns are more useful for long-term context, but no historical return guarantees future performance.']};
function learn(){const [title,copy]=topics[state.topic]||topics.intro;return `<div class="page"><div class="page-head"><span class="eyebrow">Learn</span><h1>Super, explained without the jargon.</h1><p class="lead">Learn the basics once, then use the same explanations while comparing real investment options.</p></div><div class="learn-layout"><nav class="learn-nav">${Object.entries({intro:'Super in 5 minutes',fund:'Fund vs option',growth:'Growth & risk',fees:'Fees',returns:'Returns'}).map(([k,v])=>`<button data-topic="${k}" class="${state.topic===k?'active':''}">${v}</button>`).join('')}</nav><article class="article"><h2>${title}</h2><p>${copy}</p></article></div></div>`;}
function render(){setActiveNav();const views={home,my:mySuper,compare,rankings,funds:fundsPage,learn};$('#app').innerHTML=state.view==='option'?optionPage(state.option):(views[state.view]||home)();bind();}
function bind(){document.querySelectorAll('[data-view]').forEach(b=>b.onclick=()=>go(b.dataset.view));document.querySelectorAll('[data-open-option]').forEach(b=>b.onclick=()=>go('option',{option:b.dataset.openOption}));document.querySelectorAll('[data-topic]').forEach(b=>b.onclick=()=>{state.topic=b.dataset.topic;render();});document.querySelectorAll('[data-pick-fund]').forEach(b=>b.onclick=()=>{state.fund=b.dataset.pickFund;document.querySelectorAll('[data-pick-fund]').forEach(x=>x.classList.toggle('active',x===b));const box=$('#myOptionChoices');if(box){box.innerHTML=options().filter(o=>o.fund===state.fund).map(o=>`<button class="choice" data-save-option="${o.id}">${o.option}</button>`).join('');box.querySelectorAll('[data-save-option]').forEach(x=>x.onclick=()=>{localStorage.setItem('se_option',x.dataset.saveOption);go('option',{option:x.dataset.saveOption});});}});const bi=$('#balanceInput');if(bi)bi.onchange=()=>localStorage.setItem('se_balance',String(Math.max(0,Number(bi.value)||0)));const hs=$('#homeSearch');if(hs){const box=$('#homeResults');hs.oninput=()=>{box.innerHTML=searchResults(hs.value);box.hidden=!hs.value.trim();box.querySelectorAll?.('[data-open-option]').forEach(b=>b.onclick=()=>go('option',{option:b.dataset.openOption}));};}const ca=$('#compareA'),cb=$('#compareB');if(ca)ca.onchange=()=>{state.compareA=ca.value;render();};if(cb)cb.onchange=()=>{state.compareB=cb.value;render();};document.querySelectorAll('[data-compare-option]').forEach(b=>b.onclick=()=>{state.compareA=b.dataset.compareOption;go('compare');});}
function setupGlobalSearch(){const dlg=$('#searchDialog'),open=$('#globalSearchButton'),close=$('#closeSearch'),input=$('#dialogSearchInput'),results=$('#dialogSearchResults');open.onclick=()=>{dlg.showModal();input.value='';results.innerHTML='<div class="empty">Start typing to search.</div>';setTimeout(()=>input.focus(),50);};close.onclick=()=>dlg.close();input.oninput=()=>{results.innerHTML=searchResults(input.value)||'<div class="empty">Start typing to search.</div>';results.querySelectorAll('[data-open-option]').forEach(b=>b.onclick=()=>{dlg.close();go('option',{option:b.dataset.openOption});});};dlg.addEventListener('click',e=>{if(e.target===dlg)dlg.close();});}
fetch(DATA_URL).then(r=>r.json()).then(data=>{db=data;setupGlobalSearch();render();}).catch(()=>{$('#app').innerHTML='<div class="page"><h1>Unable to load prototype data.</h1></div>';});