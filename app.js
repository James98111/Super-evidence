const FALLBACK_URL='./data/options.json';
const SUPABASE_URL='https://oaojblmkvhfeepebllxr.supabase.co';
const SUPABASE_KEY='sb_publishable_65GUUh10TfMgrtvdnQqC8A_mSXUThco';
let db={options:[],origin:'loading'};
let state={view:'home',option:null,compareA:null,compareB:null,topic:'intro'};
const $=s=>document.querySelector(s),n=v=>(v===null||v===undefined||v===''||Number.isNaN(Number(v)))?null:Number(v);
const esc=(s='')=>String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const money=v=>n(v)===null?'—':new Intl.NumberFormat('en-AU',{style:'currency',currency:'AUD',maximumFractionDigits:0}).format(n(v));
const pct=(v,d=2)=>n(v)===null?'—':`${n(v).toFixed(d).replace(/\.00$/,'').replace(/(\.\d)0$/,'$1')}%`;
const options=()=>db.options||[],option=id=>options().find(o=>o.id===id),fundNames=()=>[...new Set(options().map(o=>o.fund))].sort();
const balance=()=>Number(localStorage.getItem('se_balance')||100000);
const icon=id=>`<svg aria-hidden="true" viewBox="0 0 32 32"><use href="assets/icons.svg#${id}"></use></svg>`;
function yearsLabel(v){const x=n(v);return x===null?'Not yet verified':`${Number.isInteger(x)?x:x.toFixed(1)}+ years`}
function statusLabel(s){return({issuer_verified:'Issuer verified',regulator_verified:'Regulator verified',dual_verified:'Dual verified',derived:'Calculated',methodology_difference:'Methodology difference'})[s]||'Not yet verified'}
function perf(o,k){return n(o?.performance?.[k]?.value)}
function growth(o){return n(o?.issuerGrowth??o?.standardisedGrowthActual??o?.growth)}
function defensive(o){return n(o?.issuerDefensive??o?.defensive)??(growth(o)!==null?100-growth(o):null)}
function fee(o,b){const c=o?.costs||{},rate=n(c.adminPercent?.value)||0,cap=n(c.adminPercent?.capBalance),annual=n(c.adminPercent?.annualCap);let ap=b*rate/100;if(cap!==null)ap=Math.min(b,cap)*rate/100;if(annual!==null)ap=Math.min(ap,annual);const fixed=n(c.adminFixedAnnual?.value)||0,reserve=b*(n(c.reservePercent?.value)||0)/100,invest=b*(n(c.investmentAndTransactionPct?.value)||0)/100;return{ap,fixed,reserve,invest,total:ap+fixed+reserve+invest}}
async function supa(path){const r=await fetch(`${SUPABASE_URL}/rest/v1/${path}`,{headers:{apikey:SUPABASE_KEY,Accept:'application/json'}});if(!r.ok)throw new Error(`Supabase ${r.status}`);return r.json()}
function groupBy(rows,key){const m=new Map();for(const r of rows){const k=String(r[key]);if(!m.has(k))m.set(k,[]);m.get(k).push(r)}return m}
function normalizeDatabase(dataset,performance,allocations,fees){const pm=groupBy(performance,'option_id'),am=groupBy(allocations,'option_id'),fm=groupBy(fees,'pathway_id'),keys={12:'1y',36:'3y',60:'5y',84:'7y',120:'10y'};return dataset.map(r=>{const p={};for(const row of pm.get(String(r.id))||[]){const k=keys[Number(row.period_months)];if(k)p[k]={value:n(row.return_pct),status:row.verification_status,sourceUrl:row.source_url}}for(const k of ['1y','3y','5y','7y','10y'])if(!p[k])p[k]={value:null,status:'unverified'};const fr=fm.get(String(r.pathway_id))||[],by=x=>fr.find(y=>y.fee_name===x),actual=(am.get(String(r.id))||[]).filter(x=>x.allocation_type==='actual'),strategic=(am.get(String(r.id))||[]).filter(x=>x.allocation_type==='strategic');return{id:String(r.id),pathwayId:r.pathway_id,fund:r.fund,product:r.product||'Super product',option:r.option,style:r.style||'Investment option',option_type:r.option_type,management_style:r.management_style,risk:r.risk||'Not yet verified',suggestedTimeframe:yearsLabel(r.minimum_suggested_timeframe_years),issuerGrowth:n(r.issuer_growth_pct),issuerDefensive:n(r.issuer_defensive_pct),standardisedGrowthActual:n(r.actual_standardised_growth_pct),performance:p,costs:{adminFixedAnnual:{value:n(r.admin_fixed_annual)||0,sourceUrl:by('Account keeping fee')?.source_url},adminPercent:{value:n(r.admin_percent)||0,capBalance:n(r.admin_percent_cap_balance),annualCap:n(r.admin_percent_annual_cap)},reservePercent:{value:n(r.reserve_percent)||0},investmentAndTransactionPct:{value:n(r.investment_and_transaction_pct)||0,sourceUrl:by('Investment + transaction costs')?.source_url}},actual:actual.length?{reportingDate:actual[0].reporting_date,allocation:actual.map(x=>[x.raw_asset_label,n(x.allocation_pct)])}:null,strategic:strategic.length?{reportingDate:strategic[0].reporting_date,effective:strategic[0].effective_date,allocation:strategic.map(x=>[x.raw_asset_label,n(x.allocation_pct)])}:null,latestDataDate:r.latest_data_date}})}
function normalizeFallback(raw){return(raw.options||[]).map(o=>({...o,id:String(o.id),issuerGrowth:n(o.growth),issuerDefensive:n(o.defensive),standardisedGrowthActual:null,actual:null,strategic:o.strategic?.['2026']||null}))}
async function loadData(){try{const[d,p,a,f]=await Promise.all([supa('api_option_dataset?select=*&order=fund.asc,option.asc'),supa('api_option_performance?select=*'),supa('api_option_allocation?select=*'),supa('api_fee_rules?select=*')]);db={options:normalizeDatabase(d,p,a,f),origin:'database'}}catch(e){console.warn(e);const raw=await fetch(FALLBACK_URL).then(r=>r.json());db={options:normalizeFallback(raw),origin:'fallback'}}}
function go(view,params={}){state={...state,view,...params};render();window.scrollTo({top:0,behavior:'smooth'})}
function setActive(){document.querySelectorAll('[data-view]').forEach(b=>b.classList.toggle('active',b.dataset.view===state.view))}
function searchResults(q){q=q.trim().toLowerCase();if(!q)return'';const rows=options().filter(o=>[o.fund,o.product,o.option].some(x=>(x||'').toLowerCase().includes(q))).slice(0,12);return rows.length?rows.map(o=>`<button class="result-button" data-open-option="${o.id}"><span><strong>${esc(o.fund)} — ${esc(o.option)}</strong><small>${esc(o.product)}</small></span><span class="result-kind">Investment option</span></button>`).join(''):'<div class="empty">No verified match yet.</div>'}
const marketShelf=['AustralianSuper','Australian Retirement Trust','Hostplus','UniSuper','Aware Super','Rest','HESTA','Cbus'];
function home(){return`<div class="home"><section class="home-hero"><div class="hero-copy"><span class="eyebrow">Independent Australian super research</span><h1>Understand your super with confidence.</h1><p class="lead">Independent, easy-to-understand information about Australia’s super funds and investment options.</p><div class="hero-search"><div class="search-field">${icon('search')}<input id="homeSearch" placeholder="Search a super fund, investment option or topic" autocomplete="off"><button id="homeSearchButton">Search</button></div><div id="homeResults" class="inline-results" hidden></div></div></div><div class="hero-photo" role="img" aria-label="Professional working from home in natural light"></div></section><section class="home-actions"><button class="home-action" data-view="funds"><span class="icon-orb">${icon('search')}</span><span><h3>Find a fund</h3><p>Browse Australia’s super funds and all their investment options.</p><span class="arrow-link">→</span></span></button><button class="home-action" data-view="compare"><span class="icon-orb">${icon('compare')}</span><span><h3>Compare options</h3><p>Understand differences in fees, risk, investment mix and long-term performance.</p><span class="arrow-link">→</span></span></button><button class="home-action" data-view="learn"><span class="icon-orb">${icon('learn')}</span><span><h3>Learn about super</h3><p>Plain-English guides from the basics through to more advanced concepts.</p><span class="arrow-link">→</span></span></button></section><section class="home-funds"><div class="section-head"><div><span class="eyebrow">Australia’s super market</span><h2>Explore Australia’s super funds</h2></div><button class="section-link" data-view="funds">View all funds →</button></div><div class="fund-shelf">${marketShelf.map(name=>`<span class="fund-wordmark">${esc(name)}${fundNames().includes(name)?'<small>Data loaded</small>':''}</span>`).join('')}</div></section><section class="home-calculators"><div class="calc-intro"><span class="eyebrow">Calculators</span><h2>See what your super could be.</h2><p>Explore scenarios using transparent assumptions you can see and change.</p><button class="section-link" data-view="calculators">Explore calculators →</button></div><button class="calc-feature" data-view="calculators"><span>${icon('calculator')}</span><span><h3>Super projection calculator</h3><p>See how a balance could grow over time using explicit assumptions for returns, fees, inflation and contributions.</p></span><span class="arrow-link">→</span></button></section></div>`}
function fundsPage(){const groups=new Map();options().forEach(o=>{if(!groups.has(o.fund))groups.set(o.fund,[]);groups.get(o.fund).push(o)});return`<div class="page"><div class="page-head"><span class="eyebrow">Find a fund</span><h1>Explore the super market.</h1><p class="lead">Start with a fund, then explore the investment options available inside it.</p></div><div class="fund-list">${[...groups.entries()].map(([fund,opts])=>`<div class="fund-row"><div><h3>${esc(fund)}</h3><p class="muted">${opts.length} investment options loaded</p></div><div class="fund-options">${opts.slice(0,8).map(o=>esc(o.option)).join(' · ')}${opts.length>8?' · …':''}</div><button class="text-link" data-open-option="${opts[0].id}">Explore →</button></div>`).join('')}</div></div>`}
function compare(){const peers=options().filter(o=>perf(o,'10y')!==null),a=option(state.compareA)||peers[0]||options()[0],b=option(state.compareB)||peers[1]||options()[1];if(!a||!b)return'';const fa=fee(a,balance()),fb=fee(b,balance());return`<div class="page"><div class="page-head"><span class="eyebrow">Compare</span><h1>Compare the investment, not just the fund name.</h1><p class="lead">Start with the few measures that matter most. Open the detail only when you need it.</p></div><div class="compare-picker"><div class="field"><label>Option one</label><select id="compareA">${options().map(o=>`<option value="${o.id}" ${o.id===a.id?'selected':''}>${esc(o.fund)} — ${esc(o.option)}</option>`).join('')}</select></div><div class="vs">vs</div><div class="field"><label>Option two</label><select id="compareB">${options().map(o=>`<option value="${o.id}" ${o.id===b.id?'selected':''}>${esc(o.fund)} — ${esc(o.option)}</option>`).join('')}</select></div></div><div class="comparison">${[['',a.fund,b.fund],['10-year return',`${pct(perf(a,'10y'))} p.a.`,`${pct(perf(b,'10y'))} p.a.`],['Growth exposure',pct(growth(a),1),pct(growth(b),1)],['Risk',a.risk,b.risk],['Suggested timeframe',a.suggestedTimeframe,b.suggestedTimeframe],[`Estimated cost at ${money(balance())}`,money(fa.total),money(fb.total)]].map(r=>`<div class="compare-row"><div class="compare-label">${esc(r[0])}</div><div class="number">${esc(r[1])}</div><div class="number">${esc(r[2])}</div></div>`).join('')}</div><div class="accordion"><details><summary>Performance detail</summary><div class="detail-body">${['1y','3y','5y','7y','10y'].map(k=>`<div class="data-row"><span>${k.replace('y','-year')} return</span><strong>${pct(perf(a,k))} vs ${pct(perf(b,k))}</strong></div>`).join('')}</div></details><details><summary>Fees and portfolio context</summary><div class="detail-body"><div class="data-row"><span>${esc(a.fund)}</span><strong>${money(fa.total)}/year · ${pct(growth(a),1)} growth</strong></div><div class="data-row"><span>${esc(b.fund)}</span><strong>${money(fb.total)}/year · ${pct(growth(b),1)} growth</strong></div></div></details></div></div>`}
function optionPage(id){const o=option(id)||options()[0];if(!o)return'';const f=fee(o,balance()),alloc=o.actual||o.strategic;return`<div class="page"><div class="breadcrumb"><button data-view="funds">Funds</button> / ${esc(o.fund)}</div><section class="option-header"><div class="option-header-row"><div><span class="eyebrow">Investment option</span><h1>${esc(o.option)}</h1><p class="option-summary">${esc(o.fund)} · ${esc(o.style)}. ${growth(o)!==null?`${pct(growth(o),1)} growth exposure.`:'Growth exposure is not yet sufficiently verified.'}</p></div><button class="secondary-btn" data-compare-option="${o.id}">Compare this option</button></div><div class="metric-strip"><div class="metric"><label>10-year return</label><strong>${pct(perf(o,'10y'))}</strong><small>p.a. where verified</small></div><div class="metric"><label>Estimated annual cost</label><strong>${money(f.total)}</strong><small>at ${money(balance())}</small></div><div class="metric"><label>Growth exposure</label><strong>${pct(growth(o),1)}</strong><small>${defensive(o)!==null?`${pct(defensive(o),1)} defensive`:''}</small></div><div class="metric"><label>Risk</label><strong>${esc(o.risk)}</strong><small>Fund disclosure where loaded</small></div><div class="metric"><label>Timeframe</label><strong>${esc(o.suggestedTimeframe)}</strong><small>Fund guidance</small></div></div></section><div class="accordion"><details open><summary>Performance</summary><div class="detail-body">${['1y','3y','5y','7y','10y'].map(k=>`<div class="data-row"><span>${k.replace('y','-year')} return</span><strong>${pct(perf(o,k))}</strong></div>`).join('')}<div class="callout">Past performance is descriptive, not predictive. Longer periods are only useful when the investment strategy remained sufficiently comparable.</div></div></details><details><summary>What it invests in</summary><div class="detail-body">${alloc?.allocation?.length?alloc.allocation.map(([name,v])=>`<div class="data-row"><span>${esc(name)}</span><strong>${pct(v,2)}</strong></div>`).join(''):'<p class="muted">No sufficiently verified allocation snapshot is loaded yet.</p>'}</div></details><details><summary>Fees</summary><div class="detail-body"><div class="data-row"><span>Account keeping</span><strong>${money(f.fixed)}</strong></div><div class="data-row"><span>Asset-based administration</span><strong>${money(f.ap)}</strong></div><div class="data-row"><span>Investment + transaction</span><strong>${money(f.invest)}</strong></div><div class="data-row"><span>Costs met from reserves</span><strong>${money(f.reserve)}</strong></div><div class="data-row"><span>Total disclosed estimate</span><strong>${money(f.total)}</strong></div></div></details><details><summary>Detailed analysis and evidence</summary><div class="detail-body"><div class="data-row"><span>Data source</span><strong>${db.origin==='database'?'Live evidence database':'Static fallback'}</strong></div><div class="data-row"><span>Issuer growth exposure</span><strong>${pct(o.issuerGrowth,1)}</strong></div><div class="data-row"><span>APRA-standardised actual</span><strong>${pct(o.standardisedGrowthActual,2)}</strong></div><p class="muted">Where issuer and APRA-style classifications differ, Super Evidence preserves both rather than forcing one number to be “correct”.</p></div></details></div></div>`}
function calculators(){const b=Number(localStorage.getItem('calc_balance')||100000),c=Number(localStorage.getItem('calc_contrib')||12000),r=Number(localStorage.getItem('calc_return')||6.5),fees=Number(localStorage.getItem('calc_fee')||0.7),years=Number(localStorage.getItem('calc_years')||20),infl=Number(localStorage.getItem('calc_infl')||2.5),net=(r-fees)/100;let fv=b*Math.pow(1+net,years)+c*((Math.pow(1+net,years)-1)/net);let real=fv/Math.pow(1+infl/100,years);return`<div class="page"><div class="page-head"><span class="eyebrow">Calculators</span><h1>Explore scenarios. See every assumption.</h1><p class="lead">These calculators are illustrations, not forecasts. Nothing is hidden behind a black box.</p></div><div class="calculator-shell"><div><h2>Super projection</h2><p class="muted">Change the assumptions below. The calculation uses annual compounding and assumes contributions are made evenly through the period.</p><div class="assumptions">${[['Starting balance','calcBalance',b,1000],['Annual contributions','calcContrib',c,500],['Investment return %','calcReturn',r,.1],['Fees %','calcFee',fees,.05],['Inflation %','calcInfl',infl,.1],['Years','calcYears',years,1]].map(([l,id,v,step])=>`<div class="assumption-row"><label for="${id}">${l}</label><input id="${id}" type="number" value="${v}" step="${step}"></div>`).join('')}</div></div><div class="result-panel"><span class="eyebrow">Illustrative outcome</span><div class="big-number">${money(fv)}</div><p>Estimated future balance in nominal dollars.</p><div class="data-row"><span>In today’s dollars</span><strong>${money(real)}</strong></div><div class="data-row"><span>Net return assumption</span><strong>${pct(r-fees,2)}</strong></div><div class="data-row"><span>Inflation assumption</span><strong>${pct(infl,2)}</strong></div><div class="callout">This is a simplified illustration. Tax, contribution timing, insurance, changing returns, changing fees and legislative settings can materially change actual outcomes.</div></div></div></div>`}
function learnPage(){const topics={intro:['Start here','Super is the long-term retirement savings system. The fund is the provider; the investment option is the portfolio inside it.'],growth:['Growth assets','Growth exposure is often more informative than labels such as “Balanced” or “High Growth”. More growth usually means more short-term movement and higher expected long-run return, not a guarantee.'],returns:['Returns','One year can be noise. Longer periods are generally more useful, provided the investment strategy has remained comparable.'],fees:['Fees','Small percentage fees compound. Compare them in dollars at the same balance and separate account deductions from reserve-funded costs.'],evidence:['Evidence','Every important number should carry a source, date and verification status. Missing is better than made-up.']};const[t,x]=topics[state.topic]||topics.intro;return`<div class="page"><div class="page-head"><span class="eyebrow">Learn</span><h1>Super, without the jargon.</h1><p class="lead">Start simple. Go deeper only when you want to.</p></div><div class="learn-layout"><nav class="learn-nav">${Object.entries(topics).map(([k,v])=>`<button data-topic="${k}" class="${state.topic===k?'active':''}">${v[0]}</button>`).join('')}</nav><article class="learn-article"><h2>${t}</h2><p class="lead">${x}</p></article></div></div>`}
function about(){return`<div class="page narrow"><div class="page-head"><span class="eyebrow">About</span><h1>Independent super information, built around evidence.</h1><p class="lead">Super Evidence is designed to make the Australian superannuation market easier to understand without simplifying away the things that matter.</p></div><div class="accordion"><details open><summary>What Super Evidence covers</summary><div class="detail-body"><p>Funds, products, investment options, fees, performance, risk, portfolio composition, historical changes and public evidence.</p></div></details><details><summary>How the data works</summary><div class="detail-body"><p>Fund disclosures are kept as primary issuer evidence, APRA is used as the regulatory and standardisation layer, and platform calculations are labelled separately from raw observations.</p></div></details><details><summary>What it does not do</summary><div class="detail-body"><p>It does not treat a league-table position as personal advice and does not manufacture missing data to make a comparison look complete.</p></div></details></div></div>`}
function render(){setActive();const app=$('#app');const views={home,funds:fundsPage,compare,calculators,learn:learnPage,about};app.innerHTML=state.view==='option'?optionPage(state.option):(views[state.view]||home)();bind()}
function bind(){document.querySelectorAll('[data-view]').forEach(b=>b.onclick=()=>go(b.dataset.view));document.querySelectorAll('[data-open-option]').forEach(b=>b.onclick=()=>go('option',{option:b.dataset.openOption}));document.querySelectorAll('[data-compare-option]').forEach(b=>b.onclick=()=>go('compare',{compareA:b.dataset.compareOption}));const hs=$('#homeSearch');if(hs){const run=()=>{const r=$('#homeResults');r.innerHTML=searchResults(hs.value);r.hidden=!hs.value.trim();r.querySelectorAll('[data-open-option]').forEach(b=>b.onclick=()=>go('option',{option:b.dataset.openOption}))};hs.oninput=run;$('#homeSearchButton').onclick=run}const a=$('#compareA'),b=$('#compareB');if(a)a.onchange=()=>{state.compareA=a.value;render()};if(b)b.onchange=()=>{state.compareB=b.value;render()};document.querySelectorAll('[data-topic]').forEach(b=>b.onclick=()=>{state.topic=b.dataset.topic;render()});for(const[id,key]of[['calcBalance','calc_balance'],['calcContrib','calc_contrib'],['calcReturn','calc_return'],['calcFee','calc_fee'],['calcInfl','calc_infl'],['calcYears','calc_years']]){const el=$('#'+id);if(el)el.onchange=()=>{localStorage.setItem(key,String(Number(el.value)||0));render()}}}
function bindGlobalSearch(){const dlg=$('#searchDialog'),open=$('#globalSearchButton'),close=$('#closeSearch'),input=$('#dialogSearchInput'),results=$('#dialogSearchResults');open.onclick=()=>{dlg.showModal();input.value='';results.innerHTML='<div class="empty">Start typing a fund or investment option.</div>';setTimeout(()=>input.focus(),20)};close.onclick=()=>dlg.close();input.oninput=()=>{results.innerHTML=searchResults(input.value);results.querySelectorAll('[data-open-option]').forEach(b=>b.onclick=()=>{dlg.close();go('option',{option:b.dataset.openOption})})};dlg.addEventListener('click',e=>{if(e.target===dlg)dlg.close()})}
async function init(){$('#app').innerHTML='<div class="page"><div class="page-head"><span class="eyebrow">Loading</span><h1>Loading the evidence database…</h1></div></div>';await loadData();bindGlobalSearch();render()}
init().catch(err=>{$('#app').innerHTML=`<div class="page"><div class="callout"><strong>Unable to load Super Evidence</strong><p>${esc(err.message)}</p></div></div>`;console.error(err)});
