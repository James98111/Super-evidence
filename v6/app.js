const SUPABASE_URL='https://oaojblmkvhfeepebllxr.supabase.co';
const SUPABASE_KEY='sb_publishable_65GUUh10TfMgrtvdnQqC8A_mSXUThco';
const FALLBACK='../data/options.json';
let db={options:[]};
let state={view:'home',fund:null,option:null,a:null,b:null,balance:100000};
const $=s=>document.querySelector(s);
const esc=(s='')=>String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const num=v=>(v===null||v===undefined||v===''||Number.isNaN(Number(v)))?null:Number(v);
const pct=(v,d=1)=>num(v)===null?'—':`${num(v).toFixed(d).replace(/\.0$/,'')}%`;
const money=v=>num(v)===null?'—':new Intl.NumberFormat('en-AU',{style:'currency',currency:'AUD',maximumFractionDigits:0}).format(v);
const funds=()=>[...new Set(db.options.map(o=>o.fund))].sort();
const fundOptions=f=>db.options.filter(o=>o.fund===f);
const option=id=>db.options.find(o=>o.id===id);
const perf=(o,k)=>num(o?.performance?.[k]?.value);
const growth=o=>num(o?.issuerGrowth??o?.standardisedGrowthActual??o?.growth);
const logoDomains={
'Aware Super':'aware.com.au','Australian Retirement Trust':'australianretirementtrust.com.au','AustralianSuper':'australiansuper.com','Hostplus':'hostplus.com.au','UniSuper':'unisuper.com.au','HESTA':'hesta.com.au','Cbus':'cbussuper.com.au','Rest':'rest.com.au'
};
const logoUrl=f=>`https://img.logokit.com/${logoDomains[f]||'example.com'}`;
const market=['AustralianSuper','Australian Retirement Trust','Hostplus','UniSuper','Aware Super','Rest','HESTA','Cbus'];
const palette=['#ba694f','#6e8876','#d8b86c','#7699a5','#8e7ba4','#c78e78','#647d8c','#c8b7a2','#aab99f','#8d989f'];
function fee(o,b=state.balance){const c=o?.costs||{},r=num(c.adminPercent?.value)||0,cap=num(c.adminPercent?.capBalance),annual=num(c.adminPercent?.annualCap);let admin=b*r/100;if(cap!==null)admin=Math.min(b,cap)*r/100;if(annual!==null)admin=Math.min(admin,annual);const fixed=num(c.adminFixedAnnual?.value)||0,inv=b*(num(c.investmentAndTransactionPct?.value)||0)/100,res=b*(num(c.reservePercent?.value)||0)/100;return{admin,fixed,inv,res,total:admin+fixed+inv+res}}
function groupBy(rows,key){const m=new Map();for(const r of rows){const k=String(r[key]);if(!m.has(k))m.set(k,[]);m.get(k).push(r)}return m}
async function supa(path){const r=await fetch(`${SUPABASE_URL}/rest/v1/${path}`,{headers:{apikey:SUPABASE_KEY,Accept:'application/json'}});if(!r.ok)throw new Error(r.status);return r.json()}
function normalize(d,p,a,f){const pm=groupBy(p,'option_id'),am=groupBy(a,'option_id'),fm=groupBy(f,'pathway_id'),keys={12:'1y',36:'3y',60:'5y',84:'7y',120:'10y'};return d.map(r=>{const ps={};for(const x of pm.get(String(r.id))||[]){const k=keys[+x.period_months];if(k)ps[k]={value:num(x.return_pct),status:x.verification_status,sourceUrl:x.source_url}}for(const k of ['1y','3y','5y','7y','10y'])if(!ps[k])ps[k]={value:null};const ar=(am.get(String(r.id))||[]),actual=ar.filter(x=>x.allocation_type==='actual'),strategic=ar.filter(x=>x.allocation_type==='strategic'),fr=fm.get(String(r.pathway_id))||[];return{id:String(r.id),fund:r.fund,product:r.product||'Super',option:r.option,style:r.style||'Investment option',option_type:r.option_type,management_style:r.management_style,risk:r.risk||'Not yet verified',time:num(r.minimum_suggested_timeframe_years),issuerGrowth:num(r.issuer_growth_pct),standardisedGrowthActual:num(r.actual_standardised_growth_pct),performance:ps,costs:{adminFixedAnnual:{value:num(r.admin_fixed_annual)||0},adminPercent:{value:num(r.admin_percent)||0,capBalance:num(r.admin_percent_cap_balance),annualCap:num(r.admin_percent_annual_cap)},reservePercent:{value:num(r.reserve_percent)||0},investmentAndTransactionPct:{value:num(r.investment_and_transaction_pct)||0}},actual:actual.length?{date:actual[0].reporting_date,allocation:actual.map(x=>[x.raw_asset_label,num(x.allocation_pct)])}:null,strategic:strategic.length?{date:strategic[0].reporting_date,allocation:strategic.map(x=>[x.raw_asset_label,num(x.allocation_pct)])}:null}}
function normalizeFallback(raw){return(raw.options||[]).map(o=>({...o,id:String(o.id),issuerGrowth:num(o.growth),performance:o.performance||{},actual:null,strategic:o.strategic?.['2026']||null}))}
async function load(){try{const[d,p,a,f]=await Promise.all([supa('api_option_dataset?select=*&order=fund.asc,option.asc'),supa('api_option_performance?select=*'),supa('api_option_allocation?select=*'),supa('api_fee_rules?select=*')]);db.options=normalize(d,p,a,f)}catch(e){console.warn('database fallback',e);db.options=normalizeFallback(await fetch(FALLBACK).then(r=>r.json()))}}
function go(view,extra={}){state={...state,view,...extra};render();scrollTo({top:0,behavior:'smooth'})}
function image(f,cls=''){return `<img class="${cls}" src="${logoUrl(f)}" alt="${esc(f)} logo" onerror="this.style.display='none';this.parentElement.dataset.fallback='${esc(f)}'">`}
function riskWords(o){const g=growth(o);if(g===null)return 'Risk not yet verified';if(g>=90)return'Very high growth exposure';if(g>=75)return'High growth exposure';if(g>=55)return'Moderate growth exposure';if(g>=35)return'Lower growth exposure';return'Mostly defensive'}
function descriptor(o){if(o.option_type==='single_sector')return`${riskWords(o)} · specialist asset option`;if(o.management_style==='passive')return`${riskWords(o)} · indexed diversified`;return`${riskWords(o)} · diversified portfolio`}
function allocation(o){return o.actual||o.strategic}
function canonical(label=''){const x=label.toLowerCase();if(x.includes('australian')&&x.includes('share'))return'Australian shares';if((x.includes('international')||x.includes('global'))&&x.includes('share'))return'International shares';if(x.includes('private equity'))return'Private equity';if(x.includes('infrastructure'))return'Infrastructure';if(x.includes('property'))return'Property';if(x.includes('credit'))return'Credit';if(x.includes('fixed')||x.includes('bond'))return'Fixed income';if(x.includes('cash'))return'Cash';if(x.includes('alternative')||x.includes('unlisted'))return'Unlisted & alternatives';return'Other'}
function allocMap(o){const m=new Map();for(const [lab,val] of allocation(o)?.allocation||[]){const k=canonical(lab);m.set(k,(m.get(k)||0)+(num(val)||0))}return m}
function home(){return `<div><section class="hero wrap"><div class="hero-copy"><span class="eyebrow">Independent Australian super research</span><h1>Understand your super with confidence.</h1><p class="lead">Explore super funds, see what they invest in, and compare fees, risk and long-term performance in plain English.</p><div class="hero-search"><div class="searchbox"><span>⌕</span><input id="homeSearch" placeholder="Search a super fund" autocomplete="off"><button id="homeSearchBtn">Search</button></div><div id="homeResults" class="search-results" hidden></div><p class="hero-note">Start with the fund. We’ll show you the investment options inside it.</p></div></div><div class="hero-image" aria-label="People learning together"></div></section><section class="three-paths wrap"><button class="path" data-go="funds"><span class="path-icon">⌕</span><span><h3>Explore funds</h3><p>See each fund and the investment choices inside it.</p><span class="arrow">→</span></span></button><button class="path" data-go="compare"><span class="path-icon">↔</span><span><h3>Compare options</h3><p>See the important differences without a wall of numbers.</p><span class="arrow">→</span></span></button><button class="path" data-go="calculators"><span class="path-icon">⌁</span><span><h3>Use calculators</h3><p>Explore outcomes with assumptions you can see and change.</p><span class="arrow">→</span></span></button></section><section class="section wrap"><div class="section-heading"><div><span class="eyebrow">Australia's super market</span><h2>Explore the funds you know.</h2></div><p>Fund logos are shown for identification. Coverage will expand until every available Australian super fund is included.</p></div><div class="logo-shelf">${market.map(f=>funds().includes(f)?`<button class="fund-logo-card loaded" data-fund="${esc(f)}">${image(f)}<span>${esc(f)}</span></button>`:`<div class="fund-logo-card">${image(f)}<span>${esc(f)}</span></div>`).join('')}</div></section><section class="section wrap"><div class="section-heading"><div><span class="eyebrow">Super in 30 seconds</span><h2>Three things to understand.</h2></div></div><div class="micro-flow"><div class="micro-step"><div class="micro-visual">◎</div><strong>1. Super fund</strong><p>The organisation that holds and manages your retirement savings.</p></div><div class="flow-arrow">→</div><div class="micro-step"><div class="micro-visual">◫</div><strong>2. Investment option</strong><p>The portfolio your money is invested in inside that fund.</p></div><div class="flow-arrow">→</div><div class="micro-step"><div class="micro-visual">⌁</div><strong>3. The investments</strong><p>Shares, property, infrastructure, bonds, cash and other assets.</p></div></div></section><section class="section wrap"><div class="tool-feature"><div class="tool-copy"><span class="eyebrow">Calculators</span><h2>See the effect of small differences.</h2><p>Super is a long-term investment. Our calculators make assumptions visible so you can see what is driving the result.</p><button class="text-link" data-go="calculators">Try the projection calculator →</button></div><div class="mini-chart"><div class="mini-chart-head"><div><span>Illustrative balance after 20 years</span><strong>$506,000</strong></div><span>Example only</span></div><div class="curve"><svg viewBox="0 0 600 150" preserveAspectRatio="none"><path d="M0 136 C90 132 140 120 210 108 S355 72 430 48 S525 21 600 8" fill="none" stroke="#6e8876" stroke-width="4"/></svg></div><div class="assumption-strip"><span>6.5% return</span><span>2.5% inflation</span><span>$12k yearly contributions</span><span>Assumptions editable</span></div></div></div></section></div>`}
function searchResults(q){q=q.trim().toLowerCase();if(!q)return'';const rs=funds().filter(f=>f.toLowerCase().includes(q));return rs.length?rs.map(f=>`<button class="search-result" data-fund="${esc(f)}"><span class="logo-dot">${image(f)}</span><span><strong>${esc(f)}</strong><small>${fundOptions(f).length} options currently loaded</small></span></button>`).join(''):`<div class="empty">No verified fund match yet.</div>`}
function fundsPage(){return `<div class="page"><div class="page-head"><span class="eyebrow">Funds</span><h1>Explore Australia's super funds.</h1><p class="lead">Choose the fund first. Inside it, we’ll show the investment options and explain the differences simply.</p></div><div class="fund-directory">${funds().map(f=>`<button class="fund-tile" data-fund="${esc(f)}"><span class="logo-dot">${image(f)}</span><span><strong>${esc(f)}</strong><small>${fundOptions(f).length} investment options loaded</small></span></button>`).join('')}</div></div>`}
function category(o){if(o.option_type==='single_sector')return'Single asset & specialist';if(o.management_style==='passive')return'Indexed';if(/social|responsible|sustainable/i.test(o.option))return'Social & responsible';return'Diversified'}
function categoryHelp(k){return({Diversified:'One portfolio spread across several asset types.',Indexed:'Diversified options that mainly track market indexes and are often lower cost.','Social & responsible':'Options with additional responsible-investment or screening approaches.','Single asset & specialist':'Options focused on one asset type or a more specialised strategy.'})[k]||''}
function fundPage(f){const os=fundOptions(f),groups=new Map();for(const o of os){const k=category(o);if(!groups.has(k))groups.set(k,[]);groups.get(k).push(o)}const long=os.filter(o=>perf(o,'10y')!==null),costs=os.map(o=>num(o.costs?.investmentAndTransactionPct?.value)).filter(v=>v!==null);return `<div class="page"><div class="breadcrumbs"><button data-go="funds">Funds</button> / ${esc(f)}</div><section class="fund-hero"><div class="fund-brandbox">${image(f)}</div><div><span class="eyebrow">Super fund</span><h1>${esc(f)}</h1><p class="fund-description">Explore the investment choices inside ${esc(f)}. Start with the type of portfolio, then look at risk, long-term performance and cost.</p></div></section><div class="fund-highlights"><div><span>Investment options loaded</span><strong>${os.length}</strong></div><div><span>Options with 10-year data</span><strong>${long.length}</strong></div><div><span>Investment-cost range</span><strong>${costs.length?`${pct(Math.min(...costs),2)}–${pct(Math.max(...costs),2)}`:'—'}</strong></div></div><div class="section-heading"><div><span class="eyebrow">Investment options</span><h2>Choose the kind of portfolio first.</h2></div><p>You do not need to understand every option at once. Open a group that looks relevant, then see the simple facts for each option.</p></div>${[...groups.entries()].map(([k,rows])=>`<section class="option-category"><div class="category-title"><div><h3>${esc(k)}</h3></div><p>${esc(categoryHelp(k))}</p></div><div class="option-grid">${rows.map(o=>`<article class="option-card"><div><h3>${esc(o.option)}</h3><p class="descriptor">${esc(descriptor(o))}</p><div class="signal-row"><span class="signal risk">${pct(growth(o),0)} growth</span><span class="signal">${perf(o,'10y')!==null?`${pct(perf(o,'10y'),2)} p.a. over 10y`:'10y not yet verified'}</span><span class="signal cost">${pct(o.costs?.investmentAndTransactionPct?.value,2)} investment cost</span></div></div><button class="go" data-option="${o.id}">View →</button></article>`).join('')}</div></section>`).join('')}</div>`}
function allocationVisual(o){const m=allocMap(o),entries=[...m.entries()].filter(([,v])=>v>0).sort((a,b)=>b[1]-a[1]);if(!entries.length)return `<div class="empty">Portfolio allocation is not yet loaded for this option.</div>`;return `<div class="stacked">${entries.map(([k,v],i)=>`<span style="width:${v}%;background:${palette[i%palette.length]}" title="${esc(k)} ${pct(v)}"></span>`).join('')}</div><div class="legend">${entries.slice(0,8).map(([k,v],i)=>`<div class="legend-item"><span class="swatch" style="background:${palette[i%palette.length]}"></span><span><strong>${esc(k)}</strong><small>${pct(v)}</small></span></div>`).join('')}</div>`}
function optionPage(id){const o=option(id);if(!o)return fundsPage();const f=fee(o),g=growth(o),p10=perf(o,'10y'),periods=['1y','3y','5y','7y','10y'];const max=Math.max(...periods.map(k=>perf(o,k)||0),1);const time=o.time?`${o.time}+ years`:'Not yet verified';return `<div class="page"><div class="breadcrumbs"><button data-go="funds">Funds</button> / <button data-fund="${esc(o.fund)}">${esc(o.fund)}</button> / ${esc(o.option)}</div><section class="option-page-head"><div class="option-logo">${image(o.fund)}</div><div><span class="eyebrow">Investment option</span><h1>${esc(o.option)}</h1><p class="descriptor-large">${esc(descriptor(o))}</p></div><button class="compare-button" data-compare="${o.id}">Compare</button></section><div class="takeaway"><small>In plain English</small><p>${g!==null&&g>=75?'Most of this portfolio is invested in growth assets. That usually means bigger short-term ups and downs, with the aim of stronger growth over a long investment horizon.':'This option uses a more defensive mix than a typical high-growth portfolio. Its purpose and risk profile should be read alongside the fund’s suggested timeframe.'}</p></div><div class="four-facts"><div class="fact"><span>Risk / growth</span><strong>${pct(g,0)}</strong><small>growth exposure</small></div><div class="fact"><span>10-year return</span><strong>${p10!==null?pct(p10,2):'—'}</strong><small>${p10!==null?'p.a. historical':'not yet verified'}</small></div><div class="fact"><span>Estimated annual cost</span><strong>${money(f.total)}</strong><small>at ${money(state.balance)}</small></div><div class="fact"><span>Suggested timeframe</span><strong>${esc(time)}</strong><small>fund guidance where loaded</small></div></div><section class="visual-section"><div class="visual-intro"><div><span class="eyebrow">Portfolio</span><h2>What is your money invested in?</h2></div><p>The coloured bar shows the portfolio as a whole. We group the fund’s detailed asset labels into broad categories first, so the mix is easier to understand.</p></div>${allocationVisual(o)}</section><section class="visual-section"><div class="visual-intro"><div><span class="eyebrow">Risk</span><h2>How growth-oriented is it?</h2></div><p>Higher growth exposure generally means larger short-term market movements and greater long-term growth potential. It is not a guarantee of a better result.</p></div><div class="risk-scale"><span></span><span></span><span></span><span></span><span></span></div><div class="risk-labels"><span>More defensive</span><span>More growth-oriented</span></div></section><section class="visual-section"><div class="visual-intro"><div><span class="eyebrow">Performance</span><h2>Start with the long term.</h2></div><p>Past performance tells you what happened, not what happens next. Longer periods are more useful when the strategy has remained comparable.</p></div><div class="returns-grid"><div class="return-hero"><strong>${p10!==null?pct(p10,2):'—'}</strong><span>10-year annualised return${p10!==null?' · historical':''}</span></div><div class="return-bars">${periods.map(k=>{const v=perf(o,k);return `<div class="return-row"><span>${k.replace('y',' year')}</span><div class="bar-track"><div class="bar-fill" style="width:${v===null?0:Math.max(3,v/max*100)}%"></div></div><strong>${pct(v,2)}</strong></div>`}).join('')}</div></div></section><section class="visual-section"><div class="visual-intro"><div><span class="eyebrow">Fees</span><h2>What might it cost?</h2></div><p>Percentages can be hard to picture. Choose an example balance and we’ll translate the currently loaded disclosed fees into dollars.</p></div><div class="fee-controls">${[50000,100000,250000].map(b=>`<button data-balance="${b}" class="${state.balance===b?'active':''}">${money(b)}</button>`).join('')}</div><div class="fee-display"><strong>${money(f.total)}</strong><span>estimated disclosed cost per year at ${money(state.balance)}</span></div></section><details class="research"><summary>Research detail & sources</summary><div class="research-body"><div class="data-row"><span>Fund</span><strong>${esc(o.fund)}</strong></div><div class="data-row"><span>Product</span><strong>${esc(o.product)}</strong></div><div class="data-row"><span>Option type</span><strong>${esc(o.option_type||'—')}</strong></div><div class="data-row"><span>Management style</span><strong>${esc(o.management_style||'—')}</strong></div>${(allocation(o)?.allocation||[]).map(([k,v])=>`<div class="data-row"><span>${esc(k)}</span><strong>${pct(v,2)}</strong></div>`).join('')}<div class="data-row"><span>Account keeping component</span><strong>${money(f.fixed)}</strong></div><div class="data-row"><span>Asset-based administration</span><strong>${money(f.admin)}</strong></div><div class="data-row"><span>Investment + transaction</span><strong>${money(f.inv)}</strong></div><div class="data-row"><span>Reserve-funded cost</span><strong>${money(f.res)}</strong></div></div></details></div>`}
function selectOptions(f,id){return fundOptions(f).map(o=>`<option value="${o.id}" ${o.id===id?'selected':''}>${esc(o.option)}</option>`).join('')}
function quickTake(a,b){const out=[],ga=growth(a),gb=growth(b),fa=fee(a).total,fb=fee(b).total,ra=perf(a,'10y'),rb=perf(b,'10y');if(ga!==null&&gb!==null)out.push(Math.abs(ga-gb)<=5?`Their growth exposure is broadly similar (${pct(ga,0)} vs ${pct(gb,0)}).`:`Their growth exposure differs by ${pct(Math.abs(ga-gb),0)} — this is not a perfectly like-for-like risk comparison.`);if(fa&&fb)out.push(`${fa<fb?a.fund:b.fund} has the lower estimated disclosed cost at ${money(state.balance)}.`);if(ra!==null&&rb!==null)out.push(`${ra>rb?a.fund:b.fund} has the higher published 10-year historical return by ${Math.abs(ra-rb).toFixed(2)} percentage points p.a.`);out.push(`Look at the portfolio mix below to see where the investment exposures differ.`);return out.slice(0,4)}
function comparePage(){const fs=funds();let a=option(state.a)||fundOptions(fs[0])[0],b=option(state.b)||fundOptions(fs.find(f=>f!==a.fund)||a.fund)[0];if(!a||!b)return fundsPage();const ma=allocMap(a),mb=allocMap(b),cats=[...new Set([...ma.keys(),...mb.keys()])].filter(k=>(ma.get(k)||mb.get(k)));return `<div class="page"><div class="page-head"><span class="eyebrow">Compare</span><h1>See the differences that actually matter.</h1><p class="lead">Choose a fund and option on each side. We’ll give you a quick read first, then the visuals behind it.</p></div><div class="compare-builder"><div class="selector"><label>Fund A<select id="fundA">${fs.map(f=>`<option ${f===a.fund?'selected':''}>${esc(f)}</option>`).join('')}</select></label><label>Option A<select id="optA">${selectOptions(a.fund,a.id)}</select></label></div><div class="versus">vs</div><div class="selector"><label>Fund B<select id="fundB">${fs.map(f=>`<option ${f===b.fund?'selected':''}>${esc(f)}</option>`).join('')}</select></label><label>Option B<select id="optB">${selectOptions(b.fund,b.id)}</select></label></div></div><section class="quick-read"><span class="eyebrow">Quick read</span><h2>What stands out?</h2><div class="quick-list">${quickTake(a,b).map(x=>`<div class="quick-item"><span class="quick-icon">✓</span><span>${esc(x)}</span></div>`).join('')}</div></section><section class="compare-visual"><div class="compare-title"><div><span class="eyebrow">Essentials</span><h2>Four numbers first.</h2></div><p>These are descriptive comparisons, not a recommendation or overall score.</p></div><div class="compare-pair"><div></div><div><strong>${esc(a.fund)}</strong><br><small>${esc(a.option)}</small></div><div><strong>${esc(b.fund)}</strong><br><small>${esc(b.option)}</small></div>${[['Growth exposure',pct(growth(a),0),pct(growth(b),0)],['10-year return',perf(a,'10y')!==null?`${pct(perf(a,'10y'),2)} p.a.`:'—',perf(b,'10y')!==null?`${pct(perf(b,'10y'),2)} p.a.`:'—'],[`Annual cost at ${money(state.balance)}`,money(fee(a).total),money(fee(b).total)],['Suggested timeframe',a.time?`${a.time}+ years`:'—',b.time?`${b.time}+ years`:'—']].map(r=>`<div class="label">${esc(r[0])}</div><div><strong>${esc(r[1])}</strong></div><div><strong>${esc(r[2])}</strong></div>`).join('')}</div></section><section class="compare-visual"><div class="compare-title"><div><span class="eyebrow">Portfolio mix</span><h2>Where is the money invested?</h2></div><p>The same broad categories are used on both sides so the shapes are easy to compare.</p></div><div style="display:grid;gap:25px"><div><strong>${esc(a.option)}</strong>${allocationVisual(a)}</div><div><strong>${esc(b.option)}</strong>${allocationVisual(b)}</div></div></section><details class="research"><summary>Detailed research comparison</summary><div class="research-body">${cats.map(k=>`<div class="data-row"><span>${esc(k)}</span><strong>${pct(ma.get(k)||0)} vs ${pct(mb.get(k)||0)}</strong></div>`).join('')}</div></details></div>`}
function calculators(){let b=100000,c=12000,r=6.5,inf=2.5,admin=100,years=20;for(let y=0;y<years;y++)b=b*(1+r/100)+c-admin;const real=b/Math.pow(1+inf/100,years);return `<div class="page"><div class="page-head"><span class="eyebrow">Calculators</span><h1>Understand the assumptions, not just the answer.</h1><p class="lead">A good calculator should make it obvious what is driving the result.</p></div><div class="calculator"><div><h2>Simple super projection</h2><p class="lead">A scenario, not a forecast.</p><div class="calc-form">${[['Starting balance','100000'],['Annual contributions','12000'],['Net investment return %','6.5'],['Inflation %','2.5'],['Admin cost / year','100'],['Years','20']].map(([l,v])=>`<label class="calc-row"><span>${l}</span><input value="${v}" disabled></label>`).join('')}</div></div><aside class="calc-result"><span class="eyebrow">Illustrative result</span><div class="big">${money(b)}</div><p>Equivalent to about <strong>${money(real)}</strong> in today’s dollars at 2.5% inflation.</p><div class="takeaway"><small>Assumptions</small><p style="font-size:17px">6.5% net investment return, $12,000 annual contributions at year end, $100 annual admin cost and 2.5% inflation.</p></div><p>This prototype deliberately keeps the first view simple. The production calculator will expose tax, contribution timing, wage growth, volatility and other assumptions when relevant.</p></aside></div></div>`}
function learn(){const qs=[['01','What is a super fund?','The organisation that holds and manages your retirement savings.'],['02','What is an investment option?','The portfolio your fund uses to invest your money.'],['03','What does “High Growth” mean?','Usually a portfolio with a high share of growth assets such as shares, property and infrastructure.'],['04','How should I read returns?','Start with long periods, compare similar portfolios, and remember the past is not a forecast.'],['05','Why do fees matter?','Fees reduce the money that stays invested and compounds over time.'],['06','What is asset allocation?','The percentage of your portfolio held in different types of investments.']];return `<div class="page"><div class="page-head"><span class="eyebrow">Learn</span><h1>Super, explained when you need it.</h1><p class="lead">Short answers first. More detail only if you want it.</p></div><div class="learn-grid">${qs.map(([n,t,p])=>`<article class="learn-card"><span>${n}</span><h3>${esc(t)}</h3><p>${esc(p)}</p></article>`).join('')}</div></div>`}
function render(){const app=$('#app');const view={home,funds:fundsPage,fund:()=>fundPage(state.fund),option:()=>optionPage(state.option),compare:comparePage,calculators,learn}[state.view]||home;app.innerHTML=view();bind()}
function bind(){document.querySelectorAll('[data-go]').forEach(x=>x.onclick=()=>go(x.dataset.go));document.querySelectorAll('[data-fund]').forEach(x=>x.onclick=()=>go('fund',{fund:x.dataset.fund}));document.querySelectorAll('[data-option]').forEach(x=>x.onclick=()=>go('option',{option:x.dataset.option}));document.querySelectorAll('[data-compare]').forEach(x=>x.onclick=()=>{const a=option(x.dataset.compare),other=funds().find(f=>f!==a.fund),b=fundOptions(other||a.fund)[0];go('compare',{a:a.id,b:b?.id})});document.querySelectorAll('[data-balance]').forEach(x=>x.onclick=()=>{state.balance=+x.dataset.balance;render()});const hs=$('#homeSearch'),hr=$('#homeResults');if(hs){hs.oninput=()=>{hr.innerHTML=searchResults(hs.value);hr.hidden=!hs.value.trim();hr.querySelectorAll('[data-fund]').forEach(x=>x.onclick=()=>go('fund',{fund:x.dataset.fund}))};$('#homeSearchBtn').onclick=()=>{const f=funds().find(x=>x.toLowerCase()===hs.value.trim().toLowerCase());if(f)go('fund',{fund:f})}}const fA=$('#fundA'),fB=$('#fundB'),oA=$('#optA'),oB=$('#optB');if(fA)fA.onchange=()=>{state.a=fundOptions(fA.value)[0]?.id;render()};if(fB)fB.onchange=()=>{state.b=fundOptions(fB.value)[0]?.id;render()};if(oA)oA.onchange=()=>{state.a=oA.value;render()};if(oB)oB.onchange=()=>{state.b=oB.value;render()}}
function globalSearch(){const dlg=$('#searchDialog'),inp=$('#globalSearch'),res=$('#globalResults');$('#searchTrigger').onclick=()=>{dlg.showModal();inp.value='';res.innerHTML='<div class="empty">Search for a super fund.</div>';setTimeout(()=>inp.focus(),30)};$('#closeDialog').onclick=()=>dlg.close();inp.oninput=()=>{res.innerHTML=searchResults(inp.value);res.querySelectorAll('[data-fund]').forEach(x=>x.onclick=()=>{dlg.close();go('fund',{fund:x.dataset.fund})})};dlg.onclick=e=>{if(e.target===dlg)dlg.close()}}
async function init(){await load();globalSearch();render()}
init().catch(e=>{$('#app').innerHTML=`<div class="page"><div class="takeaway"><small>Unable to load</small><p>${esc(e.message)}</p></div></div>`})